import 'package:flutter/material.dart';

class LoadingRequestDetails extends StatelessWidget {
  const LoadingRequestDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
